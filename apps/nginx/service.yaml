apiVersion: v1
kind: Service
metadata:
  name: nginx-gitops-service
  namespace: teste
spec:
  type: NodePort
  selector:
    app: nginx-gitops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80